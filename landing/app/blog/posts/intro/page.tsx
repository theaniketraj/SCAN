import React from "react";

export default function Page() {
    return (
        <div className="container mx-auto px-6 py-8">
            <h1 className="text-3xl font-bold">Introducing SCAN</h1>
            <p className="mt-4">
                SCAN is an intelligent Gradle plugin that detects secrets and
                prevents credential leaks.
            </p>
        </div>
    );
}
